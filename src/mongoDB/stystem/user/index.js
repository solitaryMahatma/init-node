const arr = ['userName', 'createTime', 'password', 'token',
    'tel', 'phone', 'mail', 'address',
    'country', 'provinces', 'city', 'county', 'is'
]
const obj = {
    createTime: "Date",
    is: 'Boolean',
    phone: 'number'
}

const model = {
    arr: arr,
    obj: obj
}

module.exports = model